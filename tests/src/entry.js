'use strict';

// This is the entry point for your Mocha tests. The assertion library we're
// using is Should. Write tests here in `tests/src` and include them below
// like so:

require('./example');
